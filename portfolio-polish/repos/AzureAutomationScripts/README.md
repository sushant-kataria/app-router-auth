# AzureAutomationScripts

Learning scripts for Azure VM and networking automation (`newazurevm.ps1`).

The GitHub Action workflow is a demo hook and expects Azure credentials; it is not a production deploy pipeline.
