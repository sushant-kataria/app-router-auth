# azure-automation

Sample Azure PowerShell script (`create-vm.ps1`) that creates a virtual machine with `New-AzVM`.

Learning / reference only — review and set subscription, region, and credentials before running.
